<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Happy Chocolate Day ❤️</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --pink:#ff7a9c;
  --bg:#ffe6ec;
  --dark:#5c1a2b;
}

*{margin:0;padding:0;box-sizing:border-box;font-family:"Poppins",sans-serif;}

body{
  background:linear-gradient(180deg,#ffe6ec,#ffd1dc);
  height:100vh;
  overflow:hidden;
}

.wrapper{
  width:100%;
  height:100vh;
  max-width:420px;
  margin:auto;
  position:relative;
}

/* ---------- Hearts & Sparks ---------- */
.fx{
  position:fixed;
  bottom:-20px;
  animation:float 5s linear forwards;
  pointer-events:none;
}
@keyframes float{
  to{transform:translateY(-110vh) scale(1.5);opacity:0;}
}

/* ---------- Name Screen ---------- */
.screen{
  position:absolute;
  inset:0;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  padding:20px;
}

.hidden{display:none;}

input{
  padding:14px;
  width:80%;
  border-radius:30px;
  border:none;
  outline:none;
  font-size:16px;
  text-align:center;
}

button{
  margin-top:20px;
  padding:14px 30px;
  border:none;
  border-radius:30px;
  background:var(--pink);
  color:white;
  font-size:16px;
}

/* ---------- Teddy Scene ---------- */
.stage{
  position:absolute;
  bottom:180px;
  width:100%;
  height:260px;
}

.teddy{
  position:absolute;
  bottom:0;
  width:120px;
  transition:1.8s ease;
}

.bubbu{left:30px;}
.duddu{right:30px;}

svg{width:100%;}

.choco{
  position:absolute;
  bottom:90px;
  left:50%;
  transform:translateX(-50%);
  font-size:28px;
  transition:.8s;
}

.hide{opacity:0;transform:scale(0);}

/* ---------- Message ---------- */
.msg{
  position:absolute;
  bottom:110px;
  width:100%;
  text-align:center;
  font-size:20px;
  color:var(--dark);
  opacity:0;
  transition:1.5s;
}

/* ---------- Video ---------- */
video{
  position:absolute;
  bottom:0;
  width:100%;
  height:200px;
  object-fit:cover;
  border-radius:20px 20px 0 0;
  display:none;
}
</style>
</head>

<body>

<div class="wrapper">

<!-- 🎶 Background Music -->
<audio id="bgm" loop>
  <source src="music.mp3" type="audio/mpeg">
</audio>

<!-- NAME ENTRY -->
<div class="screen" id="nameScreen">
  <h2>Hey… 💗</h2>
  <p style="margin:15px 0">Enter your name to continue</p>
  <input id="nameInput" placeholder="Your name">
  <button onclick="checkName()">Continue</button>
</div>

<!-- MAIN STORY -->
<div class="screen hidden" id="story">

  <div class="stage">
    <!-- Bubbu Teddy -->
    <div class="teddy bubbu">
      <svg viewBox="0 0 100 120">
        <circle cx="50" cy="45" r="35" fill="#c98c5f"/>
        <circle cx="30" cy="20" r="12" fill="#c98c5f"/>
        <circle cx="70" cy="20" r="12" fill="#c98c5f"/>
        <circle cx="40" cy="45" r="5" fill="#000"/>
        <circle cx="60" cy="45" r="5" fill="#000"/>
        <ellipse cx="50" cy="60" rx="8" ry="6" fill="#000"/>
      </svg>
      <div class="choco hide" id="bubbuChoco">🍫</div>
    </div>

    <!-- Duddu Teddy -->
    <div class="teddy duddu">
      <svg viewBox="0 0 100 120">
        <circle cx="50" cy="45" r="35" fill="#eee"/>
        <circle cx="30" cy="20" r="12" fill="#eee"/>
        <circle cx="70" cy="20" r="12" fill="#eee"/>
        <circle cx="40" cy="45" r="5" fill="#000"/>
        <circle cx="60" cy="45" r="5" fill="#000"/>
        <ellipse cx="50" cy="60" rx="8" ry="6" fill="#000"/>
      </svg>
      <div class="choco" id="dudduChoco">🍫</div>
    </div>
  </div>

  <div class="msg" id="msg">
    Happy Chocolate Day, Harshu 🍫❤️
  </div>

  <video id="video" controls>
    <source src="video.mp4" type="video/mp4">
  </video>

</div>
</div>

<script>
const correctName="harshu";
const bgm=document.getElementById("bgm");

function blast(){
  for(let i=0;i<25;i++){
    const el=document.createElement("div");
    el.className="fx";
    el.textContent=Math.random()>0.5?"❤️":"✨";
    el.style.left=Math.random()*100+"vw";
    el.style.fontSize=(14+Math.random()*12)+"px";
    el.style.animationDuration=(4+Math.random()*2)+"s";
    document.body.appendChild(el);
    setTimeout(()=>el.remove(),6000);
  }
}

function checkName(){
  const val=document.getElementById("nameInput").value.toLowerCase().trim();
  if(val!==correctName) return;
  
  document.getElementById("nameScreen").classList.add("hidden");
  document.getElementById("story").classList.remove("hidden");
  bgm.play();
  blast();

  const duddu=document.querySelector(".duddu");
  const bubbu=document.querySelector(".bubbu");

  setTimeout(()=>{ duddu.style.right="140px"; },1000);

  setTimeout(()=>{
    document.getElementById("dudduChoco").classList.add("hide");
    document.getElementById("bubbuChoco").classList.remove("hide");
  },2600);

  setTimeout(()=>{
    duddu.style.right="180px";
    bubbu.style.left="180px";
  },4000);

  setTimeout(()=>{
    document.getElementById("msg").style.opacity=1;
    document.getElementById("video").style.display="block";
  },5600);
}
</script>
